[project]
name = "pedidos-ufcd"
version = "0.1.0"
requires-python = "==3.12.*"
dependencies = [
  "flask>=3.0",
  "openpyxl>=3.1",
  "msoffcrypto-tool>=5.0",
  "SQLAlchemy>=2.0",
  "psycopg[binary]>=3.1",
  "reportlab>=4.0",
  "python-dotenv>=1.0",
]

[tool.vercel]
entrypoint = "main.py"
